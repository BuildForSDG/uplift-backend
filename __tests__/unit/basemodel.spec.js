/*
const { mockRequest, mockResponse, mockNext } = require('../utils/httpmocks');
const BaseModel = require('../../src/app/models/base');
*/

describe('BaseModel', () => {
  describe('save', () => {
    it('Inserts a record in the database', () => {
      /*
      const baseModel = new BaseModel({
        db: {

        }
      }); */
      expect(1 + 1).toBe(2);
    });
  });
});
